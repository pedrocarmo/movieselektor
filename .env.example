# Copy to .env and fill in. The app works without either — posters won't load without
# TMDB_API_KEY, and the Overseerr link is hidden without OVERSEERR_URL.
# Get a free TMDB key at https://www.themoviedb.org/settings/api
TMDB_API_KEY=
OVERSEERR_URL=   # e.g. https://seerr.example.com
